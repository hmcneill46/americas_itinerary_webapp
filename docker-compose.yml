services:
  itinerary:
    build: .
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
    environment:
      ITINERARY_HOST: 0.0.0.0
      ITINERARY_PORT: 8765
      # ITINERARY_EDIT_TOKEN: replace-with-a-long-secret
